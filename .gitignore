# Dependencies (Thư viện)
# Bạn không bao giờ push thư mục này lên Git, nó được cài lại bằng 'npm install'
/node_modules

# Build Artifacts (File build)
# Thư mục build mặc định của Next.js. Nó được tạo ra khi chạy 'npm run build'
/.next/
/out/

# Production Artifacts (File build sản phẩm)
# File build cho 'standalone' output
/build

# Development (Phát triển)
# File chứa các biến môi trường nhạy cảm, API keys, mật khẩu...
# Luôn luôn ignore file này!
.env*.local
.env

# File log lỗi
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Cấu hình của trình biên tập (Editor Config)
.vscode/
.idea/
*.swp

# File hệ điều hành (OS Files)
.DS_Store
Thumbs.db

